export const state = () => ({})

export const getters = {}

export const mutations = {}

export const actions = {
  FETCH_ALL({ commit }) {
    return []
  },
  FETCH_VEHICLE({ commit }) {
    return {}
  }
}
